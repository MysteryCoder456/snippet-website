version: "3.7"

services:
    django_gunicorn:
        volumes:
            - static:/static
            - db:/app/db
        env_file:
            - .env
        build:
            context: .
        ports:
            - "8080:8080"

    nginx:
        build: ./nginx
        volumes:
            - static:/static
        ports:
            - "8050:8050"
        depends_on:
            - django_gunicorn

volumes:
    static:
    db:
